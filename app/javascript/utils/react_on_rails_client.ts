// Client-side only version of ReactOnRails imports  
// This file excludes server-rendering code to reduce bundle size

// Import the client-only build using the exported path
import ReactOnRails from "react-on-rails/client";

export default ReactOnRails;